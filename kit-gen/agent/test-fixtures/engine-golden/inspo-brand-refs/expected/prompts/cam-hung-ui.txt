background="transparent"
## Canvas
SQUARE 1254x1254 px, origin top-left: x grows right, y grows down. Background fully transparent: save a PNG with a real alpha channel, alpha 0 on every pixel that is not part of a drawn element.

## Art style
The attached reference image(s) ARE the style: match their rendering technique,
materials, palette and level of detail. Some of them may instead show SUBJECT
MATTER — a season, a festival, a setting, a recurring motif; borrow that from
those. Never copy the layout or composition of any reference.
Written direction, secondary to those images and never contradicting them: painterly, warm.

## Palette
Primary #101820 (dominant: primary actions and key surfaces), secondary #F2AA4C (secondary actions), gradient #101820 → #F2AA4C; neutrals derive from them. No other hues unless the art style names them.
The attached reference images decide the RENDERING, not the hue: re-tint whatever they show into this palette.

## Layout
2x2 grid of 627x627 px cells, 4 elements in reading order. Each cell is square. Keep exactly this many cells in exactly this order.
Each cell has a box, given on that element's line below: the box is the part of the cell that may be painted.
Cells are separated by empty gutters: the outer 62 px band of every cell — everything outside that box — stays completely empty, not a leaf tip, not a glow, so neighbouring elements never meet.

## Geometry
- GEOMETRY IS STRICT. Each pose is centred in its own cell and stands upright, the full figure inside its box with a clear margin above the head and below the feet.
- Draw the character as ONE natural figure, not a rim around a flat plate: no forced border, no badge frame, no plaque.
- Where a line gives a width-to-height ratio, that is the ratio of the figure itself: keep it, and never stretch or squash the character to fill space.
- Hair, tail, cape and anything the character holds come to rest inside the box on that line — characters stay inside their own box, never touch each other and never touch the image edges.
- The box on each line is a hard limit.

## Transparency
The space around the characters is simply empty: alpha 0 in the PNG, with nothing painted there. Each character's own body is solid all the way through.

## Character reference
The attached CHARACTER REFERENCE PHOTO is the character: every character cell shows EXACTLY this character — same species, face, colours, costume, materials and proportions — re-drawn in the art style above. This outranks everything else: if any other reference shows a DIFFERENT character, ignore that one.

## Layout sketch
The attached LAYOUT SKETCH is a rough composition guide: copy WHERE things sit and how much of the frame each area takes; take nothing else from it — not its style, colours, line quality or level of finish.

## Elements
The list names WHAT each cell is; the art style above decides how it looks; the core aspect ratio on its line decides its shape.
Each line also says roughly how wide that element sits on screen in the finished game. Design every stroke weight, corner radius, bevel and detail for that size, then draw it large and crisp — a small button must still read as a small button, never as a wide banner with hairline edges.
1) plate number 1 — core aspect 2:1 (about twice as wide as tall), about 240 px wide on screen — its box is x=62..564, y=62..564 (502x502 px); everything of this character, hair and props included, stays inside that box; including its hair and props it is at most 502 px wide
2) plate number 2 — core aspect 2:1 (about twice as wide as tall), about 240 px wide on screen — its box is x=689..1191, y=62..564 (502x502 px); everything of this character, hair and props included, stays inside that box; including its hair and props it is at most 502 px wide
3) plate number 3 — core aspect 2:1 (about twice as wide as tall), about 240 px wide on screen — its box is x=62..564, y=689..1191 (502x502 px); everything of this character, hair and props included, stays inside that box; including its hair and props it is at most 502 px wide
4) plate number 4 — core aspect 2:1 (about twice as wide as tall), about 240 px wide on screen — its box is x=689..1191, y=689..1191 (502x502 px); everything of this character, hair and props included, stays inside that box; including its hair and props it is at most 502 px wide

## Output
One coherent set: all 4 elements share the same style. Game-ready square 1:1 PNG with a real alpha channel (background="transparent").