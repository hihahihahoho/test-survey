/**
 * ĐÃ GỘP VỀ DÙNG CHUNG (INTEGRATION) — bản thật ở `src/lib/format.ts`.
 * Chính file này từng ghi: "Nếu sau này có 2 màn nữa cần, hãy nâng lên lib/format.ts
 * thay vì copy lần thứ ba." Đã có 3 bản ⇒ đã nâng.
 */
export { bytes, count, hhmm } from "@/lib/format";
